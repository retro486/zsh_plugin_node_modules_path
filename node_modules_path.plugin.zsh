add_nmb_to_path() {
  if [[ -d ${PWD}/node_modules/.bin ]]; then
		export OPATH=${PATH}
		export PATH=${PWD}/node_modules/.bin:${PATH}
	elif [[ ! -z ${OPATH} ]]; then
		export PATH=${OPATH}
		unset OPATH
  fi
}

autoload -U add-zsh-hook
add-zsh-hook chpwd add_nmb_to_path

add_nmb_to_path

